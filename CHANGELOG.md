# Changelog

## [Unreleased]

### Added
- Environment-based configuration using dotenv
- Support for `.env.{NODE_ENV}` and `.env.{NODE_ENV}.local` files
- Environment variables for server, JWT, MySQL, and Redis configuration
